# 视频制作新路径 — 系统性踩坑清单与防范检查表

> 本文档是 AI 导演模式（素材盘点→分镜设计→手写HTML→渲染流水线）实施过程中的参照手册。
> 每个关键步骤完成后，对照相关条目逐项检查，确保不重复前期错误。

---

## 一、图片素材类

### 1.1 图片失真
| 项目 | 内容 |
|------|------|
| **问题现象** | 图片被拉伸、压缩、模糊，分辨率与容器不匹配 |
| **历史案例** | 竖版 3:4 照片放入横版 16:9 容器，`object-fit:cover` 裁切掉 58% 内容 |
| **根因** | 容器宽高比与图片原始比例不一致时，CSS `cover` 裁切内容、`contain` 留白 |
| **防范措施** | 见下方"比例适配决策树" |

**比例适配决策树**：
```
图片比例 vs 容器比例：
├─ 保留率 ≥ 60%（如 4:3→16:9）→ object-fit:cover + 焦点裁切
├─ 保留率 < 50%（如 3:4→16:9）→ 模糊背景双层方案：
│   底层：同图 cover + blur(20px) + brightness(0.6) + scale(1.1)
│   上层：原图 contain 居中
└─ 横版→横版（如 16:9→16:9）→ 直接 cover
```

**检查时机**：分镜设计阶段确认每张图片的原始比例和目标容器比例，计算保留率。

### 1.2 容器与图片不匹配
| 项目 | 内容 |
|------|------|
| **问题现象** | 图片显示不完整、大面积空白、被截断 |
| **历史案例** | scene4/scene6 的 split 布局使用 `split-placeholder` 灰色占位区（500px），从未替换为真实图片 |
| **根因** | 编译器不知道项目目录下有什么图片，默认生成占位符 |
| **防范措施** | ① 素材盘点阶段生成完整图片清单 ② 分镜设计阶段每张图片必须指定文件名 ③ HTML 中禁止出现 `[Image]` 占位符 |

**检查时机**：HTML 制作完成后，grep 搜索 `placeholder`、`[Image]`、`src=""` 等占位标记。

### 1.3 图片语义匹配
| 项目 | 内容 |
|------|------|
| **问题现象** | 图片内容与场景旁白无关 |
| **根因** | 仅按文件名匹配，不验证图片实际内容与文案的语义关联 |
| **防范措施** | AI 导演模式下，我读取每张图片的视觉内容，与场景旁白做语义匹配确认 |

**检查时机**：分镜设计阶段，用户确认图文匹配关系。

---

## 二、时间节奏类

### 2.1 转场/停留时间过长
| 项目 | 内容 |
|------|------|
| **问题现象** | 画面静止 6-8 秒，动画入场完成后画面长时间无变化 |
| **历史案例** | 医院项目 scene1: 35s 窗口，动画 17s 完成，后 18s 几乎静止 |
| **根因** | 编译器只在入场阶段安排动画，入场完成后无持续动效 |
| **防范标准** | 见下方"场景时间设计规范" |

**场景时间设计规范**：
```
场景窗口 = TTS时长 + 入场动画时间 + 过渡缓冲

1. 窗口分配：TTS 占窗口的 60-75%，入场动画 15-25%，过渡 5-10%
2. 入场后填充：必须有持续动效（呼吸/脉冲/数据变化/循环动画）
3. 最大静止时间：任何场景内连续静止不超过 3 秒
4. 转场时间：0.8-1.2s（AGENTS.md 规范）
5. 场景间推移转场自动覆盖过渡区间，无空白
6. 内容动画起始时间 T ≥ 3（封面占 0-3s，主线从 3s 开始）
```

**持续动效模板**（AI 导演在 HTML 手写时选用，必须添加到主 timeline `tl` 上）：
```javascript
// 呼吸脉冲（适用于卡片、光晕）
tl.to(el, { scale: 1.03, opacity: 0.9, duration: 2, repeat: -1, yoyo: true, ease: "sine.inOut" }, T);

// 数据微动（适用于数字、指标）
tl.to(el, { innerHTML: targetValue * 1.02, duration: 3, repeat: -1, yoyo: true, snap: { innerHTML: 1 } }, T);

// 光线漂移（适用于背景光斑）
tl.to(el, { x: "+=30", y: "+=20", duration: 4, repeat: -1, yoyo: true, ease: "sine.inOut" }, T);

// 进度条微进（适用于指标展示）
tl.to(el, { width: "+=2%", duration: 2.5, repeat: -1, yoyo: true }, T);
```

**禁止使用独立 `gsap.to()` / `gsap.fromTo()`**，HyperFrames seek-and-capture 引擎只认 `window.__timelines["main"]`，独立调用不会被渲染捕获。

**检查时机**：分镜设计阶段审核每个场景的时间分配；HTML 完成后检查 GSAP 时间轴是否有 >3s 的无动画区间。

### 2.2 TTS 溢出场景窗口
| 项目 | 内容 |
|------|------|
| **问题现象** | 旁白音频超出场景时间窗口，导致音画不同步 |
| **已有防线** | step7_validate 自动检测（overflow_tol 随视频时长自适应：短视频 0.3s/中视频 0.5s/长视频 0.8s） |
| **前置防范** | scene_config_generator.py 已实现 TTS 驱动窗口计算；adjust_timeline.py 双向调整 |

**检查时机**：Step 0 (TTS 预生成) 后自动检测，失败时运行 adjust_timeline.py。

---

## 三、字幕与音频类

### 3.1 字幕溢出安全区
| 项目 | 内容 |
|------|------|
| **问题现象** | 字幕与场景内容重叠，或场景内容侵入字幕区域 |
| **历史案例** | 医院项目反复 4 次调整 safe-zone-height（200→280→320→325px）才通过 |
| **根因** | 编译器生成的场景内容高度不可预测，固定安全区高度不够 |
| **成功项目基准** | workflow-promo: 140px, summary: 130px, governance: 130px |

**subtitle-safe 规范（新路径）**：
```
AI 导演手写 HTML 时：
1. 字幕安全区高度：130-140px（成功项目基准，深色主题）
2. .sc 容器 padding-bottom ≥ 安全区高度 + 20px（内容自收缩布局，通常 150-160px）
3. 满高填充布局（flex:1 网格/列表撑满 .sc）：padding-bottom ≥ 240px
   - 视觉边界门禁要求内容底沿 ≤ y=840（对 y=860 安全线留 20px 余量）
   - 历史案例：集成模块隔墙项目 160px 时 6 场景底沿达 y895-918，返工一次 8 分钟渲染
4. 场景内容最大高度 = 1080 - padding-bottom（确保内容不侵入）
5. overflow:hidden 兜底（.sc 必须设置）
6. 禁止为“容纳更多内容”无限放大 safe-zone-height
   - 325px 意味着画面底部 30% 被渐变覆盖 → 视觉破坏
7. 渐变梯度参照成功项目：
   rgba(bg,0) 0% → rgba(bg,0.85) 40% → rgba(bg,0.95) 100%
   不使用过高透明度（如 0.96）导致黑色感过重
```

**检查时机**：HTML 制作后，CSS 审查 padding-bottom 和 subtitle-safe height；渲染后视觉边界检测（y=860 安全线）。

### 3.2 字幕与音频不同步
| 项目 | 内容 |
|------|------|
| **问题现象** | 字幕显示时间与旁白不匹配，提前或滞后 |
| **已有防线** | step7_validate 检查字幕 start<end、无重叠、场景覆盖 |
| **前端防范** | enhance_video_audio.py step5 使用尺度无关参数分配字幕时间 |
| **关键参数** | cover_duration 偏移量必须正确（scene0 封面场景时间偏移） |

**检查时机**：Step 2 (后处理) 后自动校验。

### 3.3 标点符号与语义不符
| 项目 | 内容 |
|------|------|
| **问题现象** | 字幕末尾出现句号、逗号等标点，影响阅读观感 |
| **已有防线** | step7_validate check4（尾部标点检测），step5 自动清除 trailing punctuation |
| **保留规则** | 仅清尾部标点（`[，、。？！：；…—,\s]+$`），保留内部语义标点 |
| **特殊处理** | 中文分号（；）必须在分割正则中（已修复） |

**检查时机**：Step 2 后自动校验。

---

## 四、视觉质量类

### 4.1 卡片尺寸与内容不协调
| 项目 | 内容 |
|------|------|
| **问题现象** | 卡片过大/过小，文字溢出或大片空白 |
| **根因** | 固定尺寸卡片不随内容量自适应 |
| **设计规范** | 见下方 |

**卡片布局规范**：
```
1. 卡片网格：max-width 1760px，居中
2. 卡片内部：padding 24-32px，gap 16-24px
3. 文字溢出策略：
   - 标题：单行，overflow:hidden + text-overflow:ellipsis
   - 正文：最多 3 行（line-clamp:3），超出缩减字号
4. 字号分层（与 HTML 模板基准一致）：
   - 封面主标题：96-108px / 权重 900
   - 场景标题：48-82px / 权重 800-900
   - 副标题：28-36px / 权重 600-700
   - 卡片标题：20-28px / 权重 700-800
   - 卡片正文：16-22px / 权重 400
   - 标签/注释：18-22px / 权重 600-700
5. 卡片数量与网格：
   - 2 列：适合 2-4 个卡片
   - 3 列：适合 5-6 个卡片
   - 4 列：适合 7-8 个卡片（字号相应缩小）
```

**检查时机**：HTML 制作后，检查每个场景的卡片数量与内容长度是否匹配网格。

### 4.2 装饰层/动画层不突出
| 项目 | 内容 |
|------|------|
| **问题现象** | 装饰元素（光晕/浮点/网格）几乎不可见 |
| **历史案例** | 医院项目 glow 用 `#0077b612`（7% alpha），dot 用 `#00b4d808`（3% alpha） |
| **根因** | 编译器统一套用极低透明度，所有场景装饰层参数完全相同 |

**装饰层规范（新路径）**：
```
AI 导演在 HTML 手写时：
1. 主光晕：opacity 0.08-0.15（与成功项目基准一致）
2. 辅助光效：opacity 0.06-0.10
3. 浮动元素：opacity 0.15-0.30
4. 每个场景的装饰参数应有差异（色相偏移、位置变化、数量调整）
5. 装饰元素底边 ≤ y=840（远离 y=860 安全线）
6. 装饰层不应喧宾夺主——为内容服务，不干扰阅读
7. 长场景（>15s）的 glow 动画用 repeat:-1 而非固定 repeat 值
```

**检查时机**：HTML 制作后，审查装饰元素的 opacity 值；instant_preview 检查动画密度。

### 4.3 底部渐变背景破坏画面
| 项目 | 内容 |
|------|------|
| **问题现象** | subtitle-safe 的深色渐变在浅色场景中形成明显"黑底" |
| **历史案例** | 医院项目（浅色 medical 主题）使用 325px 高、rgba(10,15,30,0.96) 的深色渐变 |
| **根因** | subtitle-safe 颜色固定为深色，不随场景主题适配 |

**subtitle-safe 主题适配规范**：
```
深色主题（#0f172a / #0a0e1a / #0d1117）：
  background: linear-gradient(to bottom, rgba(bg,0) 0%, rgba(bg,0.85) 40%, rgba(bg,0.95) 100%)
  height: 130-140px
  （参照成功项目：workflow-promo 140px, summary 130px, governance 130px）

浅色主题（#f8fafc / #ffffff）：
  background: linear-gradient(to bottom, rgba(0,0,0,0) 0%, rgba(0,0,0,0.35) 40%, rgba(0,0,0,0.55) 100%)
  height: 120-140px
  （用半透明黑色而非深蓝色，避免色偏）

混合主题：
  根据每个场景的背景色动态选择渐变方案
```

**检查时机**：HTML 制作后，确认 subtitle-safe 渐变颜色与场景主题匹配。

### 4.4 content_vacuum 判据被读成"入场太慢"（2026-09-23 篇1 Scene9 两次无效修复）

| 项目 | 内容 |
|------|------|
| **问题现象** | `visual_boundary_check` 报 `content_vacuum`，把 GSAP 入场时间整体提前后真实渲染仅把早期覆盖率从 10% 抬到 11%，第二次全量渲染白费 |
| **判据真实语义** | `measure_content_coverage` 量的是 y∈[120, 860] 带内**被点亮的行占比**（某行 >20 个亮像素即计入，亮像素=R/G>80 或 B>100）。采样 `early_t = start + min(7.5, dur*0.45)`、`late_t = start + dur*0.85`；FAIL 条件 `late_cov ≥ 0.15 且 early_cov < late_cov*0.35` |
| **根因** | 误以为它是"动画节奏"判据。实际是**纵向铺展度**判据：一小条标题+标签在前 7.5s 内即使全部就位，占用行数仍不足 15 行；而 `fromTo` 默认 `immediateRender:true`，seek 到 tween 之前元素恒处隐藏态 |
| **有效修复方向** | 让早期就有**大面积实心内容**立住：高卡片（不是薄条）、实心色脊、剖视图形先行。`scaleY` 类 transform 不改布局高度，"卡架先出、柱形后长"不会引发布局跳动 |
| **无效修复方向** | 单纯压缩 stagger 间隔 / 提前 tween 起点（篇1 第一次即此形态，实测 +1pp） |

**零渲染预判器（渲染前秒级裁定，替代"用全量渲染验证改动"）**：
```
python 程序文件/脚本/content_vacuum_precheck.py <index.html> [sid:start:dur ...]
# 省略场景清单时自动从 HTML 的 var T = {...} 推导全部 ≥6s 场景
```
- 原理：Chrome/Edge 无头 `--screenshot` + URL `?t=` 注入 `tl.pause(); tl.seek(t); gsap.ticker.tick()`；
  采样时刻与门禁同源，覆盖率直接调 `visual_boundary_check.measure_content_coverage`（判据不复制第二份）。
- 校准方式：先用**改前 HTML** 跑预判，读数与已有渲染帧实测对齐（篇1 实测 0.112/0.419 vs 渲染帧 0.112/0.423）后才据以裁定改后效果。
- `instant_preview.py` **不能**替代它：预览每场只取 `start+3.0` 与 `end-2.0`，与门禁采样时刻不同。
- 探针与 `gsap.min.js` 必须同目录（缺 gsap 会静默全帧空白）；工作目录取根目录同盘的 ASCII 路径（根目录含中文时 file:// 注入不可靠）。
- 退出码 2 表"无从裁定"（缺浏览器/注入失败/取数异常），不得读成通过。

**入场偏移溢出裁定（2026-09-23 补登，#1 残值）**：元素在 seek 时刻仍带 GSAP from/to 偏移、压向安全线/画布边缘（入场偏移溢出）时，以 **instant_preview 像素实测**裁定——每场 `start+3.0` / `end-2.0` 两张截图是变换后实际形态，溢出即退出 2 阻断（`:827-830`）；**不增设静态代理门**：静态 HTML 解析看不到变换后布局，增设一门只会多一个与像素分叉的裁定面（与本节"判据函数不复制第二份"同族纪律）。

---

## 五、效率与流程类

### 5.1 五个效率反模式（已记录，必须避免）
| 反模式 | 防范措施 |
|--------|---------|
| 打地鼠式修复（修一个暴露一个） | step7_validate 综合校验，一次性检查全部已知问题 |
| 不必要的全量重渲染 | 字幕/音频修改用 --quick-fix，跳过 Step 0-1 |
| 缺乏预检 | 渲染前必跑 preflight_check.py |
| 信任被污染的备份 | render_raw.mp4 是唯一可信纯净源 |
| 补丁式修复引入新问题 | 禁止 atempo 变速，用 adjust_timeline.py 调时间轴 |

### 5.2 渲染纪律
| 变更类型 | 验证手段 | 是否需要重新渲染 |
|----------|---------|:---:|
| HTML 图片替换 | preflight 确认图片存在 | 是（仅 Step 1） |
| GSAP 时间调整 | preflight 确认时间戳 | 是（Step 0-2） |
| 字幕字号/样式 | 视觉抽查 + --quick-fix | 否 |
| 旁白文本修改 | Step 0 重新 TTS | 是（Step 0-2） |
| BGM 调整 | --quick-fix | 否 |

> ⚠️ **--quick-fix 产物不可用于交付验收**：quick-fix 会把 preflight→visual_check 标记为 skipped，
> 完工报告门禁要求各步 passed=true，会拒绝认证（历史案例：集成模块隔墙项目 quick-fix 后
> 报告 FAILED，5/11 项被拒）。交付前必须全链真实跑通（--resume / --fresh）。

### 5.3 连续修复止损
- 同一问题连续修复 **超过 3 次** → 停止，输出阻塞报告
- 禁止在未获得用户新方向前继续堆叠修复尝试

### 5.4 交付审计取证命令的裸文件名陷阱（2026-09-23）
| 项目 | 内容 |
|------|------|
| **问题现象** | `generate_completion_report.py --audit --config-file prefab-decor-rules.json`（裸文件名）→ `delivery_compliance` FAIL "config unreadable"，且 `storyboard_fidelity` 误报 "narration_source not declared" |
| **根因** | `pipeline_runner.py --config` 会自动把裸名解析到 `程序文件/配置/config/pipelines/`，而完工报告的 `--config-file` **没有这层解析**，按 CWD 直读文件 |
| **防范措施** | 审计一律传全路径；误报维度修好后才允许引用其结论，不得把"取数断链"读成"项目不合格" |
| **同族** | AGENTS.md「条文存在≠生效路径」——两个入口同名参数不同实现，属参数面漂移 |

### 5.5 交付说明 md 后于 VALIDATED 创建时的补写路径
- 流水线只在 **VALIDATED 分支**写「字幕时间戳来源」节；md 当时不存在 → 日志印 `[DELIVERY-NOTES] 跳过（交付说明尚未创建）`，属设计内边界。
- 人工创建 md 后**不要**用 `--force` 重跑渲染链补写（既浪费渲染又绕过门禁）；正确动作是 in-process 调用同源函数：
  `PipelineRunner(<config全路径>).​_sync_subtitle_source_section()`
- 判据不变：内容一致时 `changed=False` 不落盘（避免刷新已交付说明 mtime 触发产物-状态一致性门禁）。

### 5.6 instant_preview 零截图 / Chrome profile 锁：诊断与恢复（2026-09-23 合并登记）

| 项目 | 内容 |
|------|------|
| **问题现象** | 两种形态：①预览报零截图或 `[COVERAGE]` 点名缺场；②Chrome 起不来、stderr 见 `The browser is already running for ...`（profile 目录被上一轮残留僵尸 chrome 占用，或陈旧 `SingletonLock` 未清） |
| **退出码语义** | `instant_preview` 退出 2 = 安全区真实溢出；**退出 3 = 预检盲区**（零截图 `instant_preview.py:833-836`、逐场缺截 `:837-841`）——**盲区非通过**：盲区里溢出多少张图都不会被看见，禁止带此状态进全量渲染；`pipeline_runner.step_preview` 视 preview 失败为 FATAL，render 硬门禁随之拒跑 |
| **活路径锁面** | main() Phase 2 走 `preview_capture.js`（puppeteer-core），userDataDir 为**固定** `PREVIEW_TEMP/chrome_profile`（`instant_preview.py:608-610`，经环境变量 `CHROME_USER_DATA_DIR`；`PREVIEW_TEMP` 默认 `D:\temp_chrome_preview`，可以 `HYPERFRAMES_PREVIEW_TEMP` 覆盖，`_script_env.py:70-71`）。固定目录 + 僵尸 chrome = SingletonLock 冲突；活路径调试端口由 puppeteer 自动分配，**9222 不是活端口** |
| **僵尸产生源** | `preview_capture.js:221-223` 写完 manifest 即 `process.exit(0)`、不 await `browser.close()`（await 会永久挂起，见同处注释）→ chrome 子进程可能残留为僵尸并持有 profile。僵尸反复出现有代码内既有防线为证：js `:104-124` HTTP 端口 8767 占用递增回退（8767-8780，注释明言"上次运行残留的僵尸进程可能占着默认端口"）、py `:660-662` 登记 2026-09-03 实测渲染进程中途崩溃只截 5/11 场；本仓 09-22 篇2 runB 渲染日志 `Target closed` ×5、发布仓 08-08 quickstart 预览日志 `EADDRINUSE` ×2 同族 |
| **死代码时代残留面** | `launch_chrome`/`get_ws_url`（`instant_preview.py:99`/`:143`，固定调试端口 **9222**）与 CLI `--port`/`--serve-port` 为死代码/死参数（main() 不消费 `args.port`，全仓零调用点；操作日志竖版适配条目同证 Phase 2 实跑走 `preview_capture.js`），其 `tempfile.mkdtemp(prefix="chrome_pv_")`（`:107`）只产生 PREVIEW_TEMP 下的**历史残留目录**。查锁时不得把 9222 当活端口排查；9222 与 `chrome_pv_*` 只是残留清理面与旧日志 grep 面 |
| **恢复顺序** | ① 确认无本人在跑的预览/渲染任务（并发 chrome 是合法持有者，不是僵尸）→ ② 清残留僵尸 chrome 进程 → ③ 清 PREVIEW_TEMP 下 `SingletonLock`（或整个 `chrome_profile` / `chrome_pv_*` 残留目录；py `:717-723` 收尾 rmtree 用 `ignore_errors=True`，被僵尸锁住的目录会留下，需人工补清）→ ④ 重跑预览，以 `[COVERAGE] …（完整）` 且退出码 0 为闭合 |
| **明文禁止** | **禁止把"自动杀 chrome"写进脚本**——kill 面无法区分僵尸 chrome 与并发任务 chrome（HyperFrames 渲染亦自带 chrome 进程），误杀即破坏他任务在跑的渲染/预览；恢复保持人工确认的手动动作。同族禁止：不得用 `--density-only` 代跑盲点重跑——该参数跳过 Phase 2 截图与安全区预检（`:833`/`:837` 两支退出 3 均被 `not args.density_only` 守护），其退出 0 不含任何安全区证据 |
| **证据分级** | 锁冲突原文逐字引自发布仓侧会话控制台；两仓 `过程产物/日志/` 递归扫描（348 + 88 份）对 `already running`/`SingletonLock` **0 命中**（控制台面未落盘，且 09-21 散文件治理已回收 32 份散日志），故该原文属**引证登记**而非日志取证；机制物理证据＝上列代码位点 + 两仓日志的 `EADDRINUSE`/`Target closed` 命中 |

---

## 六、素材感知类（新增）

### 6.1 素材盘点必做项
```
每个新项目启动前：
1. 扫描素材目录，生成清单：
   - 图片：数量、尺寸、比例、文件名
   - 文档：PPT/Word/MD，提取内容摘要
   - 视频：数量、时长、分辨率
2. 标注每个素材与哪个场景可能关联
3. 识别"无素材场景" → 决定是否用 ImageGen 生成、或纯信息排版
4. 识别"未使用素材" → 确认是有意排除还是遗漏
```

### 6.2 分镜设计审核节点
```
AI 导演在分镜设计阶段输出：
1. 每个场景的画面构成描述（用什么图、什么布局、什么动效）
2. 每个场景的时间分配（TTS 占比、动画占比、过渡占比）
3. 素材引用表（场景号 → 图片文件名 → 展示方式）

用户在此节点确认：
- 图文匹配是否正确
- 时间节奏是否合理
- 是否有遗漏的重要素材
```

---

## 七、实施检查表（逐步核对）

### 阶段 A：素材盘点（代码执行）
- [ ] 素材清单已生成
- [ ] 图片比例已标注
- [ ] 文档内容已提取摘要
- [ ] "无素材场景"已识别并标注处理方式

### 阶段 B：分镜设计（AI 导演判断）
- [ ] 每个场景有明确的画面构成方案
- [ ] 每张图片已分配到具体场景
- [ ] 图文语义匹配已确认
- [ ] 时间分配符合"TTS 占 60-75%"规范
- [ ] 无 >3s 的纯静止区间
- [ ] 字幕安全区方案已确定（深色/浅色主题）
- [ ] 用户已审核确认

### 阶段 C：HTML 制作（AI 手写）
- [ ] 无 `[Image]` 或 `placeholder` 占位符
- [ ] 所有 `<img src>` 引用真实文件
- [ ] subtitle-safe height 在 120-150px 范围
- [ ] .sc padding-bottom ≥ safe-zone-height + 20px（满高填充布局 ≥ 240px，内容底沿 ≤ y=840）
- [ ] 装饰层 opacity ≥ 0.06（主光晕 0.08-0.15）
- [ ] 每个场景装饰参数有差异
- [ ] 卡片数量与网格列数匹配
- [ ] 字号分层符合规范（标题/正文/标签梯度）
- [ ] GSAP 无 >3s 的无动画区间
- [ ] 持续动效已填充等待时间
- [ ] 同屏元素入场有 0.2-0.5s stagger
- [ ] 装饰元素底边 ≤ y=840
- [ ] 每个 ≥6s 场景跑 `content_vacuum_precheck.py` 预判为 OK（见 §4.4，早于渲染）

### 阶段 D：渲染流水线（代码执行）
- [ ] preflight_check.py 全部 PASS
- [ ] instant_preview.py 动画密度合理
- [ ] Step 1 render_raw.mp4 时长与预期匹配（偏差 <5s）
- [ ] step7_validate 全部 PASS（7 项校验）
- [ ] 视觉边界检测通过（y=860 安全线）
- [ ] 完工报告 VALIDATED（全链真实通过，禁止用 --quick-fix 产物送验）
- [ ] project_audit.py 无严重/警告项

---

## 八、成功项目参数基准

从 workflow-promo、workflow-summary、file-governance 三个成功项目中提取的参数基准：

| 参数 | workflow-promo | workflow-summary | file-governance | 推荐范围 |
|------|:---:|:---:|:---:|:---:|
| subtitle-safe height | 140px | 130px | 130px | 120-150px |
| .sc padding-bottom | 160px | 150px | 150px | 150-170px（自收缩）/ ≥240px（满高填充） |
| .sc padding-top | 50px | 80px | 80px | 50-80px |
| .sc padding-x | 100px | 120px | 120px | 100-120px |
| 场景数 | 11 | 7 | 7 | 7-11 |
| body background | #0f172a | #0d1117 | #0a0e1a | 深色系 |
| 字体 | Noto Sans SC | Noto Sans SC | Noto Sans SC | Noto Sans SC |
| glow 最大 opacity | 0.12 | 0.10 | 0.12 | 0.08-0.15 |
| ghost text opacity | 0.04 | 0.03 | 0.03 | 0.03-0.05 |

**关键发现**：成功项目的 subtitle-safe 高度均在 130-140px，而医院项目膨胀到 325px 是编译器内容溢出控制的失败，不是 safe-zone 设计的问题。

---

## 九、已知限制（不试图用代码解决）

| 限制 | 处理方式 |
|------|---------|
| Microsoft YaHei 不在 HyperFrames 确定性字体映射 | 使用 Noto Sans SC 作为主字体 |
| SDL structure 类型需用户提供 layers 结构化数据 | AI 导演模式直接处理，不依赖 SDL |
| ImageGen API 可能 40500 错误 | 降级为 CSS 渐变背景 |
| PowerShell 中文路径编码问题 | Unicode 码点构造或 ASCII 路径 |
| Node.js/Puppeteer 读中文用户名路径编码损坏 | 按需走 ASCII 跳板约定（见下方），不做持久 workaround |
| 渲染耗时 ~11 分钟不可压缩 | 用 instant_preview 提前发现问题 |

**ASCII 跳板约定**（中文用户名路径导致 Node.js/Puppeteer 编码损坏时）：
```
1. 仅在问题实际发生时临时启用，禁止预防性地长期保留 workaround
2. junction 跳板统一建在专用工具目录下（与项目根分层存放），禁止建到盘符根目录
3. 搭配会话级环境变量临时指向（PRODUCER_HEADLESS_SHELL_PATH / PUPPETEER_EXECUTABLE_PATH）
4. 任务结束当场删除指针（非递归 rmdir），不留持久 junction
5. 2026-07-30 已清理历史遗留的 D:\chrome-headless-shell，C 盘 Puppeteer 共享缓存未受影响
```
