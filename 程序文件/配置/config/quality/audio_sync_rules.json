{
  "$schema": "Audio-sync validation rules for pipeline_runner + enhance_video_audio",
  "$description": "See AGENTS.md → 渲染纪律 → 音画同步。任一场景 TTS 结束到窗口边界间的静音超过 max_seconds_per_scene → 阻断交付。",
  "dead_air": {
    "max_seconds_per_scene": 3.0,
    "fail_on_violation": true,
    "remediation": "运行 pipeline_runner.py --config <cfg>（默认已启用 --shrink）或收紧 config.json 中该场景的 end 时间。"
  },
  "shrink": {
    "enabled_by_default": true,
    "shrink_margin_seconds": 1.5,
    "description": "pipeline_runner 与 adjust_timeline.py 共同消费的 --shrink-margin 唯一权威值：TTS 播完后为镜头预留的呼吸时间。需覆盖 GSAP 场景 pre-fade(0.5s) + 感官缓冲(1.0s)，避免音频尾音撞进画面淡出。禁止任何脚本另设分叉默认值（prefab 复盘：CLI 默认 1.0s 与本值 1.5s 分叉，造成 618.9→621.7→623.8s 三次时间轴漂移）。"
  },
  "duration_consistency": {
    "max_diff_seconds": 1.0,
    "description": "后处理前置门禁（pipeline_runner.step_postprocess 早失败检查）：config video_duration 与实际渲染产物时长的绝对偏差超过该值 → 立即拒跑并给出定点修复指引（重跑 adjust_timeline 或对齐 config 后 --quick-fix）。prefab 复盘：617.8s(config) vs 621.72s(render_raw) 的漂移曾拖到 enhance step3 混音门禁才暴露，白白消耗 TTS/BGM 再生成的时间与一次 40 分钟重渲染。"
  },
  "duration_budget": {
    "description": "时长预算门禁（pipeline_runner.step_timeline 末尾检查——权威时长刚确定、渲染尚未开始的最后拦截点）：项目 config 声明 duration_budget: {seconds, enforcement} 即启用；未声明 = 门禁不生效（零副作用，预算属业务声明而非全局默认）。enforcement=soft（默认）：超限阻断，需 --accept-over-budget 显式放行并在 pipeline_state.budget_decision 留痕（超预算≠不合格，属业务决策）；hard：超限直接 FAIL。教训（2026-08-05 wp批次复盘）：4集防水视频2集超240s预算（253.0s/248.9s），当时预算只存在于需求描述、门禁完全无感知，完工报告盖章 VALIDATED 13/13 后才人工发现返工，各耗一次约25分钟全链重跑。"
  },
  "transition": {
    "tts_fade_in_ms": 40,
    "tts_fade_out_ms": 150,
    "min_inter_narration_gap_seconds": 0.5,
    "fail_on_violation": true,
    "description": "音频过渡平滑规则：每段 TTS 混音前加微淡入(40ms防爆音)+尾部淡出(150ms自然收音)；相邻场景旁白间隔（上一场景 TTS 结束→下一场景开口）低于 min_inter_narration_gap_seconds 时 step7 拦截，防听感仓促。"
  },
  "black_frame": {
    "min_black_seconds": 1.0,
    "pixel_threshold": 0.10,
    "max_overlap_with_narration_seconds": 1.0,
    "fail_on_violation": true,
    "description": "黑场-音频重叠门禁（media_qa_gate 检查14）：blackdetect 扫描成片，任一黑场区间与字幕展示窗口重叠超过 max_overlap_with_narration_seconds → 拒收（画面空档但音频持续播放）。测量对象是物理帧，不依赖任何声明值。"
  },
  "mix": {
    "tts_volume": 1.5,
    "bgm_volume": 0.08,
    "loudnorm_I": -16,
    "loudnorm_TP": -1.5,
    "loudnorm_LRA": 11,
    "description": "TTS 音量增益(1.5x) + BGM音量(0.08x) + EBU R128 响度标准化(-16 LUFS)。amix normalize=0 禁用按输入数衰减。"
  },
  "sentence_pause": {
    "min_seconds": 0.4,
    "description": "段间停顿强制（enhance_video_audio._enforce_sentence_pauses）：TTS 引擎整段合成时句间停顿由内部韵律决定（skill-promo 实测同段音频句号停顿 0~0.38s 浮动，甚至为 0），标点不构成可靠控制面。以 ASR 强制对齐段级时间戳度量相邻字幕段声学间隔，不足 min_seconds 时在边界最低能量点插入差额静音并平移后续时间戳。设 0 可关闭。"
  },
  "alignment": {
    "p95_max_seconds": 0.6,
    "min_onsets": 5,
    "silence_db": -38,
    "min_silence_seconds": 0.5,
    "fail_on_violation": true,
    "description": "字幕-语音对齐度门禁（media_qa_gate 检查12）：silencedetect 提取成片音轨语音起口，与最近字幕条目起点比对，偏差 p95 超过 p95_max_seconds 则拒收。起口数 < min_onsets（如含 BGM 无静音段）降级为警告。"
  }
}
